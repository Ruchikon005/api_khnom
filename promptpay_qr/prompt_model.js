const { DataTypes } = require('sequelize');

module.exports = model;

function model() {
    const attributes = {
        amount = parseFloat(_.get())
    };

    const Qr = attributes;

    return Qr;
}